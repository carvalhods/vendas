import { SmDropdownDirective } from './sm-dropdown.directive';

describe('SmDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new SmDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
